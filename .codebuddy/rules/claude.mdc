---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-05-11T14:01:54.294Z
provider: 
---

一、先思考，后编码
实现前明确陈述你的假设；若有不确定之处，主动提问。

如果一个问题有多种理解方式，全部列出，不要暗自选择一种。

如果存在更简单的方案，直接指出；必要时可以反对当前思路。

遇到不清晰的要求，停下来，指明困惑点并追问。

二、简单至上
只写解决当前问题所需的最少代码，不添加任何未要求的功能。

不为一次性使用的代码创建抽象层。

不要加入未要求的“灵活性”或“可配置性”。

不处理不可能出现的错误场景。

如果写了 200 行而实际只用 50 行，请重写。

三、手术式修改
只改动必须改的代码，不“顺便改进”相邻的代码、注释或格式。

不重构没问题的代码；保持原有风格，即使你不喜欢。

如果发现无关的“死代码”，可以提及，但不要删除。

你的改动产生的“孤儿”（未使用的导入、变量、函数）必须清理掉；但不要清理原有的死代码。

四、目标驱动执行
将任务转化为可验证的目标。例如：

“添加校验” → “为无效输入写测试，然后让测试通过”

“修复 bug” → “写一个能复现该 bug 的测试，然后让测试通过”

“重构 X” → “确保重构前后测试全部通过”

多步骤任务时，给出简短计划，每一步说明验证方式：

text
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
成功标准必须清晰（而非“让它工作”这种模糊要求），以便独立循环执行，减少反复澄清。

核心检验标准：

代码改动更精准，无关改动减少

避免因过度设计导致重写

澄清问题出现在实现之前，而非犯错之后